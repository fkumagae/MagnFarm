For files under `app/views`, keep PHP templates minimal and avoid adding business logic here when possible.
Small queries or counts for display-only dashboards are acceptable, but do not implement full controllers in view files.
